services:

  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: commandops_registry
      POSTGRES_USER: commandops
      POSTGRES_PASSWORD: commandops
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U commandops"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── Module Registry ─────────────────────────────────────────────────────────
  registry:
    build:
      context: ./registry
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: postgresql://commandops:commandops@postgres:5432/commandops_registry
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-prod}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin}
      VPN_PUBLIC_URL: ${VPN_PUBLIC_URL:-http://localhost:5101}
      VPN_INTERNAL_URL: http://vpn:8080
    ports:
      - "5010:8000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── VPN Module ──────────────────────────────────────────────────────────────
  vpn:
    build:
      context: ./modules/vpn
      dockerfile: backend/Dockerfile
    environment:
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-prod}
      SHELL_ORIGIN: http://localhost:5000
    ports:
      - "5101:8080"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8080/health')\""]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Gateway (nginx) ─────────────────────────────────────────────────────────
  gateway:
    image: nginx:alpine
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - vpn

  # ── Shell (frontend host) ───────────────────────────────────────────────────
  shell:
    build:
      context: ./shell
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    depends_on:
      registry:
        condition: service_healthy

volumes:
  postgres_data:
